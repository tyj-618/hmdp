server:
  port: 8081

spring:
  application:
    name: hmdp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/hmdp1?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: Tyj1314520
  data:
    redis:
      host: localhost
      port: 6379
      password: 123456
      database: 1
      connect-timeout: 3s

      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 1
          time-between-eviction-runs: 5s
  jackson:
    default-property-inclusion: non_null # JSON处理序列化时忽略null

mybatis-plus:
  type-aliases-package: hmdp.entity # 以hmdp.entity为前缀的实体类可忽略前缀

logging:
  level:
    hmdp: debug